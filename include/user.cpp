#include "user.h"

map<string, string> AdminUser::SearchStuUser(string userId, stuUser &)
{
    return map<string, string>();
}

map<string, string> AdminUser::SearchTeaUser(string userId, teaUser &)
{
    return map<string, string>();
}
